services:
  # Backend API Service (Flask)
  - type: web
    name: vaidya-backend
    runtime: python
    buildCommand: pip install -r backend_flask/requirements.txt
    startCommand: python backend_flask/app.py
    envVars:
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://vaidya-frontend.onrender.com
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /api/health

  # Frontend Static Site
  - type: static
    name: vaidya-frontend
    buildCommand: echo "No build needed"
    staticPublishPath: ./html-frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
